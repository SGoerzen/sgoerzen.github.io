/**
 * Emergency switch for optional visitor analytics.
 *
 * Keep this disabled until tracking should explicitly be re-enabled. The
 * Trackboxx environment variable alone must not activate analytics.
 */
export const TRACKBOXX_ENABLED = false;
