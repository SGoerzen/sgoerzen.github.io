---
import Card from '../Card.astro'
import { Badge } from '../ui/badge'
---

<Card colSpan="md:col-span-2 lg:col-span-1" rowSpan="md:row-span-2 lg:row-span-2" title="Fokus & Stack">
  <section class="mt-2 space-y-5" id="fokus">

    <p class="text-sm text-muted-foreground">
      Mein Schwerpunkt liegt auf XR-Lern- und Trainingssystemen, bei denen Szenariodesign, Unity- oder Three.js-Umsetzung,
      xAPI-/Telemetry-Daten, Backend-Anbindung und Auswertung sauber zusammenspielen müssen.
      Ich arbeite dort am stärksten, wo technische Entscheidungen früh über Messbarkeit, Betrieb und Wirksamkeit entscheiden.
    </p>

    <div class="flex flex-col gap-2">
      <h3 class="text-xs font-semibold uppercase tracking-wide text-muted-foreground">Kernstack</h3>
      <div class="flex w-full flex-wrap gap-2">
        <Badge variant="outline">C#</Badge>
        <Badge variant="outline">.NET</Badge>
        <Badge variant="outline">ASP.NET Core</Badge>
        <Badge variant="outline">REST APIs</Badge>
        <Badge variant="outline">PostgreSQL</Badge>
        <Badge variant="outline">Docker</Badge>
      </div>
    </div>

    <div class="flex flex-col gap-2">
      <h3 class="text-xs font-semibold uppercase tracking-wide text-muted-foreground">Produktoberflächen & Web</h3>
      <div class="flex w-full flex-wrap gap-2">
        <Badge variant="outline">TypeScript</Badge>
        <Badge variant="outline">React</Badge>
        <Badge variant="outline">Vue</Badge>
        <Badge variant="outline">Three.js / WebGL</Badge>
        <Badge variant="outline">Frontend Architecture</Badge>
      </div>
    </div>

    <div class="flex flex-col gap-2">
      <h3 class="text-xs font-semibold uppercase tracking-wide text-muted-foreground">XR, 3D & Spezialthemen</h3>
      <div class="flex w-full flex-wrap gap-2">
        <Badge variant="outline">Unity</Badge>
        <Badge variant="outline">AR/VR</Badge>
        <Badge variant="outline">Flutter + Unity</Badge>
        <Badge variant="outline">Immersal</Badge>
        <Badge variant="outline">Vuforia</Badge>
        <Badge variant="outline">xAPI</Badge>
        <Badge variant="outline">Learning Analytics</Badge>
      </div>
    </div>

    <div class="flex flex-col gap-2">
      <h3 class="text-xs font-semibold uppercase tracking-wide text-muted-foreground">Ergänzende Erfahrung</h3>
      <div class="flex w-full flex-wrap gap-2">
        <Badge variant="outline">AWS</Badge>
        <Badge variant="outline">Azure</Badge>
        <Badge variant="outline">Supabase</Badge>
        <Badge variant="outline">MongoDB</Badge>
        <Badge variant="outline">CI/CD</Badge>
        <Badge variant="outline">.NET MAUI</Badge>
        <Badge variant="outline">AvaloniaUI</Badge>
        <Badge variant="outline">WPF</Badge>
        <Badge variant="outline">Flutter</Badge>
        <Badge variant="outline">Go</Badge>
        <Badge variant="outline">C++</Badge>
      </div>
    </div>

    <div class="flex flex-col gap-2">
      <h3 class="text-xs font-semibold uppercase tracking-wide text-muted-foreground">Einsatz</h3>
      <div class="flex w-full flex-wrap gap-2">
        <Badge variant="outline">Remote deutschlandweit</Badge>
        <Badge variant="outline">Vor Ort im Raum Köln</Badge>
      </div>
    </div>

  </section>
</Card>
